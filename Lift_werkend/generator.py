class Generator:
    def __init__(self, _elevators):
        self.elevators = _elevators

    def genUpdate(self, updaters):
        def update(self):
            controller.

        return _update

    def genSweep(self, updaters):
        for i in range(len(self.elevators)):
            self.__setattr__('update{}'.format(i), genUpdate)

        def sweep(self):
            for i in range(len(elevators)):
                pass

        return _sweep
